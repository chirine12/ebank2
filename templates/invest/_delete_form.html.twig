<form method="post" action="{{ path('app_invest_delete', {'id': invest.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ invest.id) }}">
    <button class="btn">Delete</button>
</form>
