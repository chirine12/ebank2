<form method="post" action="{{ path('app_typetaux_delete', {'id': typetaux.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ typetaux.id) }}">
    <button class="btn">Delete</button>
</form>
