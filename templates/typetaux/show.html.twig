{% extends 'base.html.twig' %}

{% block title %}Typetaux{% endblock %}

{% block body %}
    <h1>Typetaux</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typetaux.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ typetaux.type }}</td>
            </tr>
            <tr>
                <th>Taux</th>
                <td>{{ typetaux.taux }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_typetaux_index') }}">back to list</a>

    <a href="{{ path('app_typetaux_edit', {'id': typetaux.id}) }}">edit</a>

    {{ include('typetaux/_delete_form.html.twig') }}
{% endblock %}
