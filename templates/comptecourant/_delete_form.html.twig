<form method="post" action="{{ path('app_comptecourant_delete', {'id': comptecourant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comptecourant.id) }}">
    <button class="btn">Delete</button>
</form>
