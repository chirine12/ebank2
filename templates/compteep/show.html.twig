{% extends 'base.html.twig' %}

{% block title %}Compteep{% endblock %}

{% block body %}
    <h1>Compteep</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ compteep.id }}</td>
            </tr>
            <tr>
                <th>Rib</th>
                <td>{{ compteep.Rib }}</td>
            </tr>
            <tr>
                <th>Solde</th>
                <td>{{ compteep.solde }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ compteep.type }}</td>
            </tr>
            <tr>
                <th>Dateouv</th>
                <td>{{ compteep.dateouv ? compteep.dateouv|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ compteep.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_compteep_index') }}">back to list</a>

    <a href="{{ path('app_compteep_edit', {'id': compteep.id}) }}">edit</a>

    {{ include('compteep/_delete_form.html.twig') }}
{% endblock %}
