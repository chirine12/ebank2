{{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': 'novalidate'}}) }}

<div class="row">
    <div class="col-xl-6">
        <div class="mb-3 row">
            {{ form_label(form.nom, 'Nom', {'label_attr': {'class': 'col-lg-4 col-form-label'}}) }}
            <div class="col-lg-6">
                {{ form_widget(form.nom) }}
                {{ form_errors(form.nom) }}
            </div>
        </div>
        
        <div class="mb-3 row">
            {{ form_label(form.prenom, 'Prénom', {'label_attr': {'class': 'col-lg-4 col-form-label'}}) }}
            <div class="col-lg-6">
                {{ form_widget(form.prenom) }}
                {{ form_errors(form.prenom) }}
            </div>
        </div>
        
        <div class="mb-3 row">
            {{ form_label(form.Rib, 'RIB', {'label_attr': {'class': 'col-lg-4 col-form-label'}}) }}
            <div class="col-lg-6">
                {{ form_widget(form.Rib) }}
                {{ form_errors(form.Rib) }}
            </div>
        </div>
    </div>
</div>

<div class="mb-3 row">
    <div class="col-lg-8 ms-auto">
        <button type="submit" class="btn btn-default">Soumettre</button>
    </div>
</div>

{{ form_end(form) }}